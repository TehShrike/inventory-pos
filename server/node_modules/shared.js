var fs = require('fs')
var camelize = require('camelize')

fs.readdirSync('shared')
	.filter(file => /^.+\.js$/.test(file))
	.map(filename => {
		var name = camelize(filename.substring(0, filename.length - '.js'.length))

		module.exports[name] = require('../../shared/' + filename)
	})
