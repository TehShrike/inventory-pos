var wrapPromise = require('wrap-promise')
var denodeify = require('then-denodeify')

var db = require('db')

module.exports = function saveAddPlantDocument(userId, addPlantDocument, cb) {
	// bring the redux store into a shared place so that I can use it here to add accountId
	// and tagScope

	// add accountId and userId to appropriate tables!
	db.plant.saveAddPlantDocument(addPlantDocument, cb)
}
